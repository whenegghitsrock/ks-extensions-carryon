{{- if .Values.crds.update }}
{{- $crdFiles := .Files.Glob "crds/**" }}
{{- $i := 0 }}
{{- range $path, $_ := $crdFiles }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent.fullname" $ }}-crds-{{ $i }}
  namespace: {{ include "agent.namespace" $ }}
  labels:
    {{- include "agent.labels" $ | nindent 4 }}
  annotations:
    {{- include "agent.updateCrdsHook.annotations" $ | nindent 4 }}
data:
  {{ $path | base }}: |
    {{- $.Files.Get $path | nindent 4 }}
---
{{- $i = add1 $i }}
{{- end }}

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "agent.fullname" . }}-update-crds
  labels:
    {{- include "agent.labels" . | nindent 4 }}
  annotations:
    {{- include "agent.updateCrdsHook.annotations" $ | nindent 4 }}
rules:
  - apiGroups: 
    - apiextensions.k8s.io
    resources: 
    - customresourcedefinitions
    verbs:
    - get
    - create
    - update
    - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "agent.fullname" . }}-update-crds
  labels:
    {{- include "agent.labels" . | nindent 4 }}
  annotations:
    {{- include "agent.updateCrdsHook.annotations" $ | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "agent.fullname" . }}-update-crds
subjects:
  - kind: ServiceAccount
    name: {{ include "agent.fullname" . }}-update-crds
    namespace: {{ include "agent.namespace" . }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agent.fullname" . }}-update-crds
  labels:
    {{- include "agent.labels" . | nindent 4 }}
  annotations:
    {{- include "agent.updateCrdsHook.annotations" $ | nindent 4 }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "agent.fullname" . }}-update-crds
  namespace: {{ include "agent.namespace" . }}
  labels:
    {{- include "agent.labels" . | nindent 4 }}
  annotations:
    {{- include "agent.updateCrdsHook.annotations" $ | nindent 4 }}
spec:
  template:
    spec:
      restartPolicy: Never
      serviceAccountName: {{ include "agent.fullname" . }}-update-crds
      volumes:
        {{- $i = 0 }}
        {{- range $path, $_ := $crdFiles }}
        - name: crds-{{ $i }}
          configMap:
            name: {{ include "agent.fullname" $ }}-crds-{{ $i }}
            items:
            - key: {{ $path | base }}
              path: {{ $path | base }}
        {{- $i = add1 $i }}
        {{- end }}
      containers:
        - name: update-crds
          image:  {{ template "agent.kubectl.image" . }}
          command:
            - /bin/bash
            - -c
            - |
              kubectl apply -f /data/crds 2>&1
          {{- $i = 0 }}
          volumeMounts:
          {{- range $path, $_ := $crdFiles }}
            - name: crds-{{ $i }}
              mountPath: /data/crds/{{ $path | base }}
              subPath: {{ $path | base }}
          {{- $i = add1 $i }}
          {{- end }}
          resources:
            {{- toYaml .Values.kubectl.resources | nindent 12 }}
{{- end }}